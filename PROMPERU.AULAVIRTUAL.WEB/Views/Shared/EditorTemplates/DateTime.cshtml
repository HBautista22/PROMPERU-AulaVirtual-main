@model DateTime?

<div class="form-group">
    @Html.LabelForModel(new { @class="col-sm-3 control-label"})
    <div class="col-sm-5">
        @Html.DateTextBoxFor(x => x, new { @class="form-control datepicker"})
        @Html.ValidationMessageFor(x => x)
    </div>
</div>